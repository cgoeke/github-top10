import { createMuiTheme } from "@material-ui/core";

export const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#24292e",
      contrastText: "#fff"
    },
    secondary: {
      main: "#0366d6",
      constrastText: "#fff"
    },
    background: {
      default: "#f6f8fa"
    }
  }
});
